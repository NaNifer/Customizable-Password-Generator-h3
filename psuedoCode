generateBtn opens dialog boxes
    askNums1 prompt asks for a number
        If no number is entered, second prompt opens
            askNums2 prompt specifies that a number is required
                If number is not entered, askNums2 is put into a loop
                Else if, askNums2 = number, 
                    put string into chosenNums
                    go to lowerCaseAsk confirm
        Else if, askNums1 = number, 
            put string into chosenNums
            go to lowerCaseAsk confirm

    lowerCaseAsk confirm asks if user wants to use lowercase
        Once button has been pressed, 
            open upperCaseAsk confirm

    upperCaseAsk confirm asks if user wants to use uppercase
        Once button has been pressed, 
            open numericAsk confirm

    numericAsk confirm asks if user wants to use numeric characters
        Once button has been pressed, 
            open specialAsk confirm

    specialAsk confirm asks if user wants to use special characters
        Once button has been pressed, 
            open summaryParameters alert


    ' diplays chosenNums + any true result from confirm boxes as a message
    summaryParameters = chosenNums + any true result from (chosenLower, chosenUpper, chosenNumeric, chosenSpecial)

            If lowerCaseAsk = true, then display text "Lowercase"
                Else if go to chosenUpper
            If upperCaseAsk  = true, then display text "Uppercase"
                Else if go to chosenNumeric
            If numericAsk = true, then display text "Numeric"
                Else if go to chosenSpecial
            If specialAsk = true, then display text "Special"

summaryParameters alert "click", call on function generatePassword ();
    


chosenNums = number that the user entered
    Convert chosenNums string to number
chosenLower = true/false
chosenUpper = true/false
chosenNumeric = true/false
chosenSpecial = true/false

generatePassword ();
    inputs string into 
